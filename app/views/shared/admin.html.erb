<div style="margin:100px;">
  <div id="just-font" > 
  
  <h1 id="head-text-color">User list</h1><br>
   
  <% if current_user && current_user.superadmin? %>
  
  <!-- <table id='user-list' class="table table-striped table-bordered table-hover font-weight-bold">
  </table> -->
  <!-- <p>

    <strong>User:</strong>
    <%= current_user.username %>
  </p> -->

  <table id='userTable' class="table table-striped table-bordered table-hover text-white font-weight-bold">
    <thead class="text-dark"> 
      <tr>
        <th>Profile</th>
        <th>name</th>
        <th>uid</th>
        <th>provider</th>
        <th>email</th>
        <th>user</th>
        <th>Action</th>
        
      </tr>
    </thead>
    <tbody class="text-dark">
     
      <% @user = User.find(6) %>
      <!-- <%= form_for :@user do |f| %> -->
      <tr>
        <td><%= image_tag(@user.image) %></td>
        <td><%= @user.name %></td>
        <td><%= @user.email  %></td>
        <td><%= @user.provider %></td>
        <td><%= @user.email %></td>
        <td><%= @user.role %></td>
        <!-- <td><%= link_to 'Action', method: :post, data: { confirm: 'Are you sure?' }, :remote => true, :class => 'block_user' , class:"text-dark"%></td> -->
      </tr>

      <!-- <% end %> -->
    </tbody>
  </table>
    <% end %><br>
    </div>
  </div>  
</div>