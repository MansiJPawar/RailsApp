<div style="margin:100px;">
    <div id="just-font" > 
      <p id="notice"><%= notice %></p>
    <h1 id="head-text-color">User list</h1><br>
   
    <% if current_user && current_user.superadmin? %>
    
    <!-- <table id='user-list' class="table table-striped table-bordered table-hover font-weight-bold">
    </table> -->
    

  <table id='userTable' class="table table-striped table-bordered table-hover text-white font-weight-bold">
    <thead class="text-dark"> 
      <tr>
        <th>name</th>
        <th>uid</th>
        <th>provider</th>
        <th>email</th>
        <th>user</th>
      </tr>
    </thead>
    <tbody class="text-dark">
      <tr>
        <td><%= current_user.name %></td>
        <td><%= current_user.uid  %></td>
        <td><%= current_user.provider %></td>
        <td><%= current_user.email %></td>
        <td><%= current_user.role %></td>
      </tr>
    </tbody>
  </table>
    <% end %><br>
    </div>
  </div>  
</div>