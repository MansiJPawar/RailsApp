<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
     
    <title>Instagram</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>
  <body>
      <!-- Responsive navbar-->
      <nav class="navbar navbar-expand-lg navbar-light bg-white nav-tab">
        <div class="container">
            <!-- user signed in direct to dashboardpath -->
            <a class="navbar-brand" href="<%= account_signed_in? ? dashboard_path : root_path %>">
              <i class="fa fa-instagram"></i>  | Instagram
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ms-auto m b-2 mb-lg-0">
                 <% if account_signed_in? %>
                   <%= render "layouts/account_nav"%>
                 <% else %>
                   <%= render "layouts/public_nav"%>
                 <% end %>
                </ul>
            </div>
        </div>
    </nav>
    <!-- Page content-->
    <div class="container">
        <div class="mt-5">
          <%= yield %>  
        </div>
    </div>
  </body>
</html>